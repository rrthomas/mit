language: c

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages: &default_deps
      - texlive-latex-extra
      - texlive-science
      - texlive-fonts-recommended
      - texlive-fonts-extra
      - help2man
      - latexmk
      - hevea

# env:
#   global:
#     - VERBOSE=1

matrix:
  include:
    - os: linux
    - os: osx
    - env:
        - WORD_SIZE=4
        - WORD_SIZE=8
    - compiler: gcc
      env:
        - COMPILER=gcc-7
        - CFLAGS="-g3 -fsanitize=address -fsanitize=undefined"
        - LDFLAGS="-fsanitize=address -fsanitize=undefined"
        - PYTHONMALLOC=malloc
        - PY_LOG_ENV="LD_PRELOAD=/usr/lib/gcc/x86_64-linux-gnu/7/libasan.so"
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - *default_deps
            - [gcc-7]

script:
  - ./bootstrap && ./configure --enable-silent-rules
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install help2man ; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then make check ; else make distcheck ; fi
